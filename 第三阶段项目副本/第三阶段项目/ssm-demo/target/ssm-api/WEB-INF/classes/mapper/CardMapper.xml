<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cykj.mapper.CardMapper">

    <insert id="batchInsertCards" parameterType="java.util.List">
        INSERT INTO cy_card
        (card_prefix, card_number, card_money, card_is_delete, card_status, card_create_time, card_update_time)
        VALUES
        <foreach collection="cards" item="card" separator=",">
            (#{card.cardPrefix}, #{card.cardNumber}, #{card.cardMoney},
             #{card.cardIsDelete}, #{card.cardStatus}, #{card.cardCreateTime}, #{card.cardUpdateTime})
        </foreach>
    </insert>
</mapper>
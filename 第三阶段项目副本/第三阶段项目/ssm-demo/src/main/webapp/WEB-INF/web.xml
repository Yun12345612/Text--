<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
         http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd"
         version="3.1">

    <!-- 1. 配置Spring核心容器的配置文件路径 -->
    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>classpath:spring-mybatis.xml</param-value>
    </context-param>


    <!-- 2. CORS跨域过滤器（已适配动态Origin） -->
    <filter>
        <filter-name>corsFilter</filter-name>
        <filter-class>com.cykj.config.SimpleCorsFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>corsFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>


    <!-- 3. 字符编码过滤器（解决中文乱码） -->
    <filter>
        <filter-name>characterEncodingFilter</filter-name>
        <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
        <init-param>
            <param-name>forceEncoding</param-name>
            <param-value>true</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>characterEncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>


    <!-- 4. 配置Spring上下文监听器（加载Spring核心容器） -->
    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>


    <!-- 5. 配置SpringMVC的DispatcherServlet -->
    <servlet>
        <servlet-name>dispatcherServlet</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <param-value>classpath:springmvc.xml</param-value>
        </init-param>
        <!-- 项目启动时立即加载Servlet -->
        <load-on-startup>1</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>dispatcherServlet</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>


    <!-- 6. 配置静态资源访问（通过默认Servlet访问外部图片） -->
    <servlet-mapping>
        <servlet-name>default</servlet-name>
        <url-pattern>/external-images/*</url-pattern>
    </servlet-mapping>


    <!-- 7. 配置Session（关键：让Cookie在所有路径共享） -->
    <session-config>
        <!-- Session超时时间（30分钟） -->
        <session-timeout>30</session-timeout>
        <!-- Cookie配置：路径设为根目录，所有子路径共享 -->
        <cookie-config>
            <path>/</path>
            <http-only>false</http-only> <!-- 允许前端读取Cookie（可选） -->
            <max-age>-1</max-age> <!-- 浏览器关闭后Cookie失效 -->
        </cookie-config>
        <!-- 仅通过Cookie跟踪Session（禁用URL重写） -->
        <tracking-mode>COOKIE</tracking-mode>
    </session-config>

</web-app>
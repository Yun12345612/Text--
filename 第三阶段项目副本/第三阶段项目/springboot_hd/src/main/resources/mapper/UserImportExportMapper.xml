<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.springboot_hd.mapper.UserImportExportMapper">

    <!-- 检查手机号是否存在 -->
    <select id="existsByPhone" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM user
        WHERE user_phone = #{phone}
          AND (user_is_delete IS NULL OR user_is_delete = '0')
    </select>

    <!-- 检查身份证号是否存在 -->
    <select id="existsByIdCard" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM user
        WHERE user_card = #{idCard}
          AND (user_is_delete IS NULL OR user_is_delete = '0')
    </select>

    <!-- 检查账号是否存在 -->
    <select id="existsByAccount" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM user
        WHERE user_account = #{account}
          AND (user_is_delete IS NULL OR user_is_delete = '0')
    </select>

</mapper>